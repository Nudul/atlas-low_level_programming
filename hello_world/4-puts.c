#include <stdio.h>


/* Main will print a string with the quote at the beginning included */
int main(void)
{
	/* Prints the string with puts */
	puts("\"Programming is like building a multilingual puzzle");
	/* Returns to 0 to check success */
	return (0);
}
